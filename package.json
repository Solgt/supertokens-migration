{
  "name": "supertokens-migration",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "migrate": "bun run scripts/users-migrate.ts",
    "migrate-user": "bun run scripts/user-migrate.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}